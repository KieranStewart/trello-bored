# Notes for Running VSCode Extension

1. Open the `trelloboredextension` folder directly in VSCode
2. To run the mock api server for testing purposes, run `node mock-api.js`
3. Run through debugger on the `src/extension.ts` file
4. In VScode pop-out window for viewing the extension, select the `Check List App`